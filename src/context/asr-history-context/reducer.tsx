export const asrHistoryInitialState = {
  list: {
    name: '牛逼啊',
  },
};

/**
 * History Panel State Reducer
 * @param previousState
 * @param action
 * @constructor
 */
export const AsrHistoryReducer = (previousState: any, action: any) => {
  switch (action.type) {
    default:
      return previousState;
  }
};
